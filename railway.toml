[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && python -m gunicorn vaultERP.wsgi --log-file - --bind 0.0.0.0:$PORT"
healthcheckPath = "/healthz/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
